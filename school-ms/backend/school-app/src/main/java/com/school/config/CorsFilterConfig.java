package com.school.config;

// Deprecated duplicate CORS registration. Intentionally left without @Configuration
// and without beans to avoid duplicate registrations or ambiguous bean resolution.
// CORS is handled via WebCorsConfig + Spring Security http.cors().
final class CorsFilterConfig { /* no-op */ }
